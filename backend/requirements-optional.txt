# Optional dependencies for YouTube Genius
# Install with: pip install -r requirements-optional.txt

# ──────────────────────────────────────────────
# LOCAL WHISPER TRANSCRIPTION
# ──────────────────────────────────────────────
# Only needed when cloud transcription (Groq/Gemini) is unavailable
# or when processing videos without captions.
# Note: Downloads ~150MB+ model files on first use
#
# faster-whisper is recommended (faster, lower memory)
faster-whisper>=1.0.0

# Standard OpenAI Whisper (fallback if faster-whisper unavailable)
# openai-whisper>=20231117

# ──────────────────────────────────────────────
# DOCUMENT PROCESSING
# ──────────────────────────────────────────────
# For analyzing uploaded PDF/DOCX files
pypdf>=4.0.0
python-docx>=1.1.0

# ──────────────────────────────────────────────
# OCR (Optical Character Recognition)
# ──────────────────────────────────────────────
# For extracting text from scanned PDFs/images
# SYSTEM DEPENDENCIES REQUIRED:
#   Ubuntu/Debian: apt-get install tesseract-ocr poppler-utils
#   macOS: brew install tesseract poppler
#   Windows: Download installers from GitHub
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
